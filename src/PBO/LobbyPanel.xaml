<UserControl x:Class="PokemonBattleOnline.PBO.LobbyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PokemonBattleOnline.PBO.Lobby"
             MinHeight="300" MinWidth="280" BorderBrush="Black" Background="#a8d5e7">
    <Grid>
        <Rectangle Name="gridbg" IsHitTestVisible="False"/>
        <local:LobbyView x:Name="lobby"/>
        <local:Login x:Name="login" VerticalAlignment="Center">
            <local:Login.Effect>
                <DropShadowEffect Opacity="0.5" ShadowDepth="0" BlurRadius="20" Color="White"/>
            </local:Login.Effect>
        </local:Login>
    </Grid>
</UserControl>
